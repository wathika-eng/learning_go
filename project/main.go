package main

func main() {
	// prices4
	prices := []float64{10, 20, 30}
	taxRates := []float64{0, 0.7, 0.1, 0.15}
	results := make(map[float64][]float64)
	for index, taxtaxRate := range taxRates {
		for priceIndex, price := range prices {
		}
	}
}
